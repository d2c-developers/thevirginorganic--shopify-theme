<div class="">
  <div class="grid grid-cols-1 xl:grid-cols-2 xl:divide-x divide-border">
    {% for block in section.blocks %}
      {% if block.type == 'product' %}
        {% assign product = block.settings.product %}
        <div class="flex flex-col items-center ">
          <a href="{{ product.url }}" class="flex flex-col items-center max-w-md mx-auto">
            <div class="relative flex items-center justify-center">
              {% if product.featured_image %}
                {{ product.featured_image | image_url: width: 800 | image_tag: class: 'w-full h-full object-contain' }}
              {% else %}
                <div class="text-sm"></div>
              {% endif %}
            </div>
            <div class="mt-4 text-center pl-5 xl:pl-0 w-full">
              <span class="text-dark-grey font-serif italic">{{ product.metafields.tvo.tagline }}</span>
              <h3
                class="font-sans text-2xl uppercase tracking-[0.15em]"
                style="color: {{ product.metafields.tvo.label_color }};"
              >
                {{ product.title }}
              </h3>

              <div class="pt-5 text-grey-50">
                {% if product.metafields.tvo.harvest != blank %}
                  <div class="flex justify-between border-b border-border py-[10px] pr-5 xl:pr-0 ">
                    <span class="text-sm font-sans tracking-[0.15em]">HARVEST</span>
                    <span class="font-serif italic">{{ product.metafields.tvo.harvest }}</span>
                  </div>
                {% endif %}

                {% if product.metafields.tvo.polyhenial_count != blank %}
                  <div class="flex justify-between border-b border-border py-[10px] pr-5 xl:pr-0 ">
                    <span class="text-sm font-sans tracking-[0.15em]">Polyphenol Count</span>
                    <span class="font-serif italic">{{ product.metafields.tvo.polyhenial_count }}</span>
                  </div>
                {% endif %}

                {% if product.metafields.tvo.intensity != blank %}
                  <div class="flex justify-between items-start border-b border-border py-[10px] pr-5 xl:pr-0 ">
                    <span class="text-sm font-sans tracking-[0.15em]">INTENSITY</span>
                    <span class="text-right max-w-[220px] font-serif italic leading-tight">
                      {{ product.metafields.tvo.intensity | newline_to_br }}
                    </span>
                  </div>
                {% endif %}
              </div>
            </div>
          </a>
        </div>
      {% endif %}
    {% endfor %}
  </div>

  <div class="flex items-center justify-center mt-[50px]">
    {%
      render 'ui-button',
      button_text: section.settings.button_text | default: 'Shop All',
      type: 'link',
      href: section.settings.button_url | default: routes.all_products_collection_url
    %}
  </div>
</div>

{% schema %}
{
  "name": "Featured Products",
  "class": "my-100",
  "settings": [
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop All"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Featured Product",
      "limit": 2,
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Products",
      "settings": {
        "button_text": "Shop All"
      }
    }
  ]
}
{% endschema %}
